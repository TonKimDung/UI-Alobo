
  # Mobile interface adaptation

  This is a code bundle for Mobile interface adaptation. The original project is available at https://www.figma.com/design/eSNkzUIwP8Snstf2u0g29y/Mobile-interface-adaptation.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  